const Index = require('../index.js');

describe('Index', () => {
    describe('userInput', () => {
        it('should not accept characters that is more than the length of 3', () => {

        })
    })
});